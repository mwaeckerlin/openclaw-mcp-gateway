{
  "name": "openclaw-mcp-gateway",
  "version": "1.0.0",
  "description": "MCP server to give SSH-sandboxed AI agents limited access to the OpenClaw Gateway API.",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:docker": "docker compose build",
    "test": "tsx --test src/commands.test.ts src/cron.test.ts src/disabled-tools.test.ts src/skills.test.ts src/server.test.ts src/server.integration.test.ts && (cd test && docker compose up --build --force-recreate --remove-orphans --abort-on-container-exit --exit-code-from test-client)",
    "start": "docker compose up --build --force-recreate --remove-orphans",
    "start:daemon": "docker compose up -d --build --force-recreate --remove-orphans",
    "dev": "tsx src/server.ts"
  },
  "keywords": [
    "mcp",
    "openclaw",
    "gateway",
    "security"
  ],
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "^22.17.0",
    "@types/ws": "^8.18.1",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
